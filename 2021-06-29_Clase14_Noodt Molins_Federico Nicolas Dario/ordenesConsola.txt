 -function movimiento(paso) {
 	 objeto = document.getElementById("rectangulo")
         tope = parseInt(objeto.style.top)
         tope += parseInt(paso)
         ancho = parseInt(objeto.style.left)
         ancho += parseInt(objeto.style.top)
         concatenadoTOP = tope + "px"
         concatenadoLEFT = ancho + "px"
         objeto.style.top = concatenadoTOP
         objeto.style.left = concatenadoLEFT
         return objeto.style.top, objeto.style.left
         }
 
 -movimiento(parámetro)
 
 -setInterval(movimiento, milisegundos, parámetro)
